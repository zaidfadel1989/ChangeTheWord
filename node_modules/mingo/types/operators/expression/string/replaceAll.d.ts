import { ExpressionOperator } from "../../../core";
/**
 * Replaces all instances of a matched string in a given input.
 *
 * @param  {AnyObject} obj
 * @param  {Array} expr
 */
export declare const $replaceAll: ExpressionOperator;

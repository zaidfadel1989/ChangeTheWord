import { ExpressionOperator } from "../../../core";
/**
 * Replaces the first instance of a matched string in a given input.
 *
 * @param  {AnyObject} obj
 * @param  {Array} expr
 */
export declare const $replaceOne: ExpressionOperator;

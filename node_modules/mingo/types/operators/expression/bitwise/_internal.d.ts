import { Options } from "../../../core";
import { Any, AnyObject } from "../../../types";
export declare function processBitwise(obj: AnyObject, expr: Any, options: Options, compute: (n: number[]) => number): number;

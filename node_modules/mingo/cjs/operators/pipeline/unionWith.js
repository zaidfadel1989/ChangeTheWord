var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var unionWith_exports = {};
__export(unionWith_exports, {
  $unionWith: () => $unionWith
});
module.exports = __toCommonJS(unionWith_exports);
var import_internal = require("../../aggregator/_internal");
var import_lazy = require("../../lazy");
var import_util = require("../../util");
var import_internal2 = require("./_internal");
const $unionWith = (collection, expr, options) => {
  const { coll, pipeline: p } = (0, import_util.isString)(expr) || (0, import_util.isArray)(expr) ? { coll: expr } : expr;
  const arr = (0, import_util.isString)(coll) ? options.collectionResolver(coll) : coll;
  const { documents, pipeline } = (0, import_internal2.filterDocumentsStage)(p ?? [], options);
  (0, import_util.assert)(
    !arr !== !documents,
    "$unionWith: must specify single collection input with `expr.coll` or `expr.pipeline`."
  );
  const xs = arr ?? documents;
  return (0, import_lazy.concat)(
    collection,
    pipeline ? new import_internal.AggregatorImpl(pipeline, options).stream(xs) : (0, import_lazy.Lazy)(xs)
  );
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  $unionWith
});

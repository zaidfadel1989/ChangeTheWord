import { isNil } from "../../util";
const $mergeObjects = (collection, _expr, _options) => {
  const acc = {};
  for (const o of collection) {
    if (isNil(o)) continue;
    for (const k of Object.keys(o)) {
      if (o[k] !== void 0) acc[k] = o[k];
    }
  }
  return acc;
};
export {
  $mergeObjects
};

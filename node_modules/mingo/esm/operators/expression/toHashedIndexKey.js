import { computeValue } from "../../core";
import { hashCode } from "../../util";
const $toHashedIndexKey = (obj, expr, options) => hashCode(computeValue(obj, expr, null, options), options.hashFunction);
export {
  $toHashedIndexKey
};
